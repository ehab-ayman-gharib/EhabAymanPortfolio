/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['catbox.moe', 'files.catbox.moe'], // add all external hosts here

}
};

export default nextConfig;
